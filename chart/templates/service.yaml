apiVersion: v1
kind: Service
metadata:
  name: {{ include "streamio-addons.fullname" . }}
  labels:
    {{- include "streamio-addons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "streamio-addons.selectorLabels" . | nindent 4 }}
